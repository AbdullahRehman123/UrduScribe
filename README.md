# Urdu-Audio-Transcription-Microservice
A Python-based microservice that transcribes Urdu audio files (`.wav` or `.mp3`) using a **locally fine-tuned Whisper ASR model**. Supports batch processing, chunked transcription for long audios, and daily rotated logs.
